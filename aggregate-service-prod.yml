external:
  # В проде используем переменные окружения для указания логинов, паролей и урлов
  discovery-service-url: ${DISCOVERY_SERVICE_URL}
  # в проде увеличиваем таймаут
  zipkin-endpoint: ${ZIPKIN_URL}
  loki-url: ${LOKI_URL}
  menu:
    rest-client:
      read-timeout: 5s
  review:
    rest-client:
      read-timeout: 5s
